# Iris_dataset
Built a machine learning classification model using Logistic Regression to predict iris flower species based on sepal and petal measurements. Preprocessed data with Label Encoding, evaluated performance using accuracy metrics, and deployed the trained model using pickle for reuse.
